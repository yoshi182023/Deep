GenMail

*LLM powered email features*

Requirements:
- *uv*: python package manager
- *npm*: javascript package manager

Start the server:

- `cd server`
- `uv sync`
- `uv run python main.py`

Start the client:

- `cd client`
- `npm install`
- `npm run dev`